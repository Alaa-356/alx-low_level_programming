#include "main.h"
/**
 * add - entry point
 * Description: function that adds two integers and returns the result.
 * @n: is variable
 * @m: is variable
 * Return: return result
 */
int add(int n, int m)
{
	int res;

	res = n + m;
	return (res);
}
