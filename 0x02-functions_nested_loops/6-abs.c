#include "main.h"
/**
 * _abs - to print alphabet ac
 * Description:  function that checks for lowercase character.
 * @n: is variable take from the other function
 * Return: o
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
